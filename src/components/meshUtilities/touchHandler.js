import React, { Component } from "react";
import { View } from "react-native";

type Pos = [number, number];

export default class TouchPositionResponding extends Componeng {
  state = {
    touching: false,
    touchPosition: initialPosition
    //touchAngle: 0
  };
}
